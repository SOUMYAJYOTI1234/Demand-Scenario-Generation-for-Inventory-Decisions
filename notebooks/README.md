# Notebooks

Numbered, exploratory only: `01-eda.ipynb`, `02-...`. Notebooks explore;
`src/demand_vae/` implements. Anything used twice gets promoted from a
notebook into the package (roadmap Phase 3).

Planned:
- `01-eda.ipynb` (Phase 4): weekly demand distributions with NB/Poisson/Gaussian
  fits overlaid, intermittency/zero-fraction analysis, seasonal profiles,
  price/promo effects.
